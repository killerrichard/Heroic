{
  "name": "angular-electron",
  "version": "0.0.0",
  "license": "MIT",
  "main": "index.js",
  "scripts": {
    "debug": "nodemon --watch * --exec electron src"
  },
  "dependencies": {
    "electron-reload": "^1.2.2",
    "express": "^4.16.2"
  }
}
